'use client';

import { styled } from '@mui/material/styles';

export const RootWrapper = styled('div')({
  backgroundColor: '#2C2C2C',
  minHeight: '100vh',
  width: '100%',
  margin: 0,
  padding: 0,
}); 